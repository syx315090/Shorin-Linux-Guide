# SHORiN Linux Guide —— 工具清单与配置说明

本文档基于项目中的 Debian 安装脚本 (`scripts/debian-install.sh`) 和 `legacy/` 目录下的历史配置文件，整理出项目所安装、配置的全部工具及其作用。

---

## 目录

1. [Wayland 桌面核心](#1-wayland-桌面核心)
2. [系统基础服务](#2-系统基础服务)
3. [桌面 Shell 组件](#3-桌面-shell-组件)
4. [终端与 Shell 工具](#4-终端与-shell-工具)
5. [中文输入法](#5-中文输入法)
6. [截图与剪贴板](#6-截图与剪贴板)
7. [硬件控制与媒体](#7-硬件控制与媒体)
8. [开发与文本工具](#8-开发与文本工具)
9. [游戏与性能监控](#9-游戏与性能监控)
10. [主题与外观](#10-主题与外观)
11. [Arch/Hyprland 专属（legacy）](#11-archhyprland-专属legacy)
12. [配置目录速查表](#12-配置目录速查表)

---

## 1. Wayland 桌面核心

### 1.1 Niri —— 可滚动平铺 Wayland 合成器

**作用**：管理所有窗口的显示、布局和交互。Niri 的特色是"无限向右滚动"的平铺布局——新窗口总是在右侧创建，用户可以左右滚动工作区，类似在一个无限长的卷轴上排列窗口。

**使用情境**：
- 日常多任务：左边浏览器查文档，中间编辑器写代码，右边终端跑命令，用 `Mod+滚轮` 平滑滚动
- 工作区隔离：`Mod+1~9` 切换 9 个工作区，分别放开发、聊天、娱乐等不同场景
- 浮动窗口：`Mod+V` 将文件管理器或设置面板切换为浮动模式

**本项目中配置位置**：
- Debian：由脚本动态生成 → `~/.config/niri/config.kdl`（模板在 `scripts/debian-install.sh` 的 `install_niri_config()` 函数内）
- 截图辅助脚本：`legacy/.config/niri/scripts/`

### 1.2 SDDM —— 登录管理器

**作用**：提供图形化登录界面，管理用户会话的启动。

**使用情境**：
- 开机后看到 SDDM 登录界面，输入密码
- 在会话菜单中选择 "Niri" 进入 Wayland 桌面
- 如果系统有多个桌面环境（如同时安装了 GNOME），可在这里切换

**本项目**：脚本自动安装并启用 SDDM，同时生成 `/usr/share/wayland-sessions/niri.desktop` 注册 Niri 会话。

---

## 2. 系统基础服务

### 2.1 PipeWire / WirePlumber —— 音视频服务

**作用**：替代传统 PulseAudio，管理所有音频输入输出（扬声器、麦克风、蓝牙耳机），以及屏幕录制/投屏的视频流。

**使用情境**：
- 插上耳机自动切换输出设备，无需手动配置
- 用 OBS 录屏时，PipeWire 提供低延迟的音视频流
- 蓝牙耳机连接后自动识别为音频输出

**本项目**：脚本安装 `pipewire`、`wireplumber`、`pipewire-pulse`、`pipewire-alsa`。

### 2.2 NetworkManager —— 网络管理

**作用**：管理有线/Wi-Fi/VPN 网络连接，提供命令行 (`nmcli`) 和图形界面 (`network-manager-gnome`) 两种操作方式。

**使用情境**：
- 点击 waybar 上的网络图标，选择 Wi-Fi 并输入密码
- 终端中用 `nmcli dev wifi connect "SSID" password "密码"` 连 Wi-Fi
- 插入网线自动切换为有线连接

**本项目**：安装并启用为 systemd 服务，附带 GNOME 托盘组件。

### 2.3 BlueZ / Blueman —— 蓝牙

**作用**：管理蓝牙设备连接（耳机、音箱、鼠标、键盘、手柄等）。

**使用情境**：
- 打开 Blueman 图形界面，搜索并配对蓝牙耳机
- 蓝牙鼠标自动重连

**本项目**：安装 `bluez`、`blueman`，启用 `bluetooth` 服务。

### 2.4 CUPS —— 打印服务

**作用**：管理本地和网络打印机。

**使用情境**：
- 连接 USB 打印机后自动识别
- 通过浏览器访问 `http://localhost:631` 管理打印任务

**本项目**：安装 `cups`，启用服务。

### 2.5 Flatpak / Flathub —— 沙盒应用分发

**作用**：跨发行版的软件安装方式，应用运行在沙盒中互不干扰。

**使用情境**：
- `flatpak install flathub com.obsproject.Studio` 安装 OBS 录屏软件
- `flatpak install flathub com.valvesoftware.Steam` 安装 Steam
- 应用自动更新：`flatpak update`

**本项目**：安装 `flatpak`，自动添加 Flathub 远程仓库。

### 2.6 XDG Desktop Portal —— 桌面门户

**作用**：为沙盒应用（Flatpak）提供屏幕截图、文件选择、屏幕录制等系统功能的统一接口。

**使用情境**：
- Flatpak 版 Firefox 弹出文件选择对话框 → 由 xdg-desktop-portal-gtk 提供
- OBS 请求屏幕录制权限 → 由 xdg-desktop-portal + Niri 内置 screencast 支持提供

**本项目**：安装 `xdg-desktop-portal`、`xdg-desktop-portal-gtk`，并生成 `/usr/share/xdg-desktop-portal/niri-portals.conf`。

---

## 3. 桌面 Shell 组件

### 3.1 Waybar —— 状态栏

**作用**：在屏幕顶部显示工作区列表、时间、音量、网络、电池等信息。

**使用情境**：
- 看时间日期（顶栏中央）
- 查看当前工作区（顶栏左侧的编号）
- 点击 "SHORiN" 文字启动应用启动器 (fuzzel)
- 监控电池剩余电量
- `Mod+O` 隐藏/显示顶栏

**本项目配置位置**：
- 主配置：`~/.config/waybar/config-niri`（脚本生成）
- 样式表：`~/.config/waybar/style.css`（脚本生成）

### 3.2 swaybg —— 壁纸渲染

**作用**：在 Niri 桌面背景上显示静态壁纸。Niri 本身不渲染任何背景，必须借助 swaybg 等外部程序。

**使用情境**：
- 开机后自动显示壁纸，否则桌面就是一片漆黑
- 更换壁纸：修改配置中的图片路径

**本项目**：Niri 配置中通过 `spawn-sh-at-startup` 自动启动，壁纸默认使用 `~/Pictures/Shorin-Wallpapers/wallhaven-vpq7m8.png`。

### 3.3 swaylock —— 屏幕锁定 / hyprlock（legacy Hyprland 用）

**作用**：锁屏界面，需要输入密码才能解锁。

**使用情境**：
- `Mod+Alt+L` 手动锁屏
- 系统空闲 10 分钟后自动锁屏（由 swayidle 触发）

**本项目**：`swaylock` 用于 Debian/Niri，`hyprlock` 是旧 Arch/Hyprland 配置中的对应锁屏工具。

### 3.4 swayidle —— 空闲管理 / hypridle（legacy）

**作用**：监听用户空闲状态，触发锁屏、息屏、挂起等动作。

**使用情境**：
- 离开电脑 10 分钟 → 自动锁屏
- 离开 30 分钟 → 自动挂起到内存
- 合上笔记本盖子 → 锁屏 + 挂起

**本项目配置位置**：
- Niri 版：直接在 `config.kdl` 中通过 `spawn-sh-at-startup` 配置
- Hyprland 旧版：`legacy/.config/hypr/hypridle.conf`

### 3.5 wlogout —— 注销/关机菜单

**作用**：提供一个图形化的系统操作菜单（锁屏、注销、休眠、关机、重启）。

**使用情境**：
- 按快捷键弹出菜单 → 选择关机
- 离开座位前选择锁屏

**本项目配置位置**：
- 布局定义：`legacy/.config/wlogout/layout`
- 样式：`legacy/.config/wlogout/style.css`
- 颜色：`legacy/.config/wlogout/colors.css`
- 图标：`legacy/.config/wlogout/icons/`

### 3.6 mako —— 通知守护进程

**作用**：在桌面右上角显示系统和应用通知弹窗。

**使用情境**：
- 收到新邮件 → mako 弹出通知
- 音量调节 → 显示音量变化
- 截图完成 → 提示截图已保存
- 蓝牙设备连接/断开 → 状态通知

**本项目**：Niri 配置中通过 `spawn-at-startup` 自动启动。

### 3.7 fuzzel —— 应用启动器

**作用**：类似 macOS Spotlight / Windows 开始菜单，输入应用名快速启动。

**使用情境**：
- `Mod+Z` 打开 fuzzel → 输入 "fire" → Enter → 启动 Firefox
- 执行计算器、设置等不常用的程序，不需要翻菜单

**本项目**：Niri 配置中 `Mod+Z` 绑定到 fuzzel。

### 3.8 niriswitcher —— Alt+Tab 窗口切换器

**作用**：为 Niri 提供经典的 Alt+Tab 窗口/工作区切换界面（Niri 本身没有类似功能）。

**使用情境**：
- `Alt+Tab` 切换到上一个窗口
- 看到所有窗口的缩略图 + 名称，快速定位

**本项目配置位置**：
- 主配置：`legacy/.config/niriswitcher/config.toml`
- 样式：`legacy/.config/niriswitcher/style.css`
- 颜色：`legacy/.config/niriswitcher/colors.css`

---

## 4. 终端与 Shell 工具

### 4.1 终端模拟器：kitty / foot / ghostty

**作用**：提供命令行终端窗口。

| 终端 | 特点 | 本项目配置位置 |
|------|------|---------------|
| kitty | GPU 加速，功能丰富 | Niri 默认终端 (`Mod+T`) |
| foot | 极简、轻量、Wayland 原生 | `legacy/.config/foot/foot.ini` |
| ghostty | 新兴 GPU 终端，性能优秀 | `legacy/.config/ghostty/config` |

**使用情境**：
- `Mod+T` 打开 kitty → 运行各种命令
- 用 fish 替代 bash，享受自动补全和语法高亮

### 4.2 fish —— 现代化 Shell

**作用**：比 bash 更友好的交互式 Shell，自带语法高亮、自动补全、命令历史搜索。

**使用情境**：
- 输入 `git sta` 按 Tab → 自动补全为 `git stash`
- 输入部分命令按 ↑ → 智能历史搜索
- 默认使用 fish 作为登录 Shell

**本项目**：通过 `chsh -s /usr/bin/fish` 或脚本安装后手动切换。

### 4.3 starship —— Shell 提示符美化

**作用**：跨 Shell 的提示符主题引擎，显示 Git 分支、命令执行时间、Python 版本等上下文信息。

**使用情境**：
- 在 Git 仓库中，提示符显示当前分支名和修改状态
- 命令执行时间超过阈值时显示耗时

**本项目**：在 `.zshrc` 中配置 `eval "$(starship init zsh)"`（legacy）。

### 4.4 eza —— 现代化 `ls`

**作用**：`ls` 的现代替代品，支持图标、颜色、Git 状态、树形展示。

**使用情境**：
- `ls` → 彩色文件列表 + 图标
- `la` → 详细列表，目录优先
- `lt` → 树形展示，最多两层

**本项目配置**：`.zshrc` 中 `alias ls='eza --icons'`。

### 4.5 zoxide —— 智能 `cd`

**作用**：记忆你访问过的目录，根据使用频率自动跳转。

**使用情境**：
- `z proj` → 跳转到最常访问的包含 "proj" 的目录（如 `~/Projects/Shorin-Linux-Guide/`）
- `z` → 交互式选择常用目录

**本项目配置**：`.zshrc` 中 `alias cd="z"`。

### 4.6 fzf —— 模糊搜索

**作用**：通用的模糊搜索工具，可配合任何命令使用。

**使用情境**：
- `Ctrl+R` → 模糊搜索命令历史
- `vim $(fzf)` → 模糊搜索文件名并用 vim 打开
- `kill -9 $(ps aux | fzf | awk '{print $2}')` → 交互式杀进程

### 4.7 fastfetch —— 系统信息显示

**作用**：在终端打印系统信息（发行版、内核、CPU、GPU、内存、桌面环境等）。

**使用情境**：
- 每次打开终端时自动显示系统概览
- 截图晒桌面时展示系统配置

**本项目配置**：`.zshrc` 中 `alias fa='fastfetch'`。

### 4.8 btop —— 系统资源监控

**作用**：终端中的图形化资源管理器，显示 CPU、内存、磁盘、网络、进程信息。

**使用情境**：
- 编译大型项目时在另一个终端开 btop，看 CPU 温度和使用率
- 排查哪个进程在大量占用内存

---

## 5. 中文输入法

### 5.1 fcitx5 —— 输入法框架

**作用**：管理中文输入法的启动、切换和候选词显示。

**使用情境**：
- 按 `Ctrl+Space` 切换中英文
- 在浏览器、编辑器、终端中直接输入中文

**本项目**：
- 安装 `fcitx5`、`fcitx5-frontend-gtk3/gtk4/qt5`（确保在所有应用中可用）
- 环境变量脚本：`/etc/profile.d/shorin-fcitx5.sh`
- Niri 配置中自动启动 `fcitx5`

### 5.2 fcitx5-rime —— Rime 输入引擎

**作用**：高质量的中文输入引擎，支持拼音、双拼、五笔、粤拼等方案。

**使用情境**：
- 默认使用雾凇拼音方案（全拼）
- 输入长句时带上 `,` `.` `?` `!` 等标点，AI 辅助断句

**本项目配置位置**：
- Rime 自定义配置：`legacy/.local/share/fcitx5/rime/rime_ice.custom.yaml`
  - 扩充了输入字符集，允许在拼音中直接输入标点
  - 配置了 LLM 辅助翻译器，利用 AI 提升长句准确率

### 5.3 fcitx5-chinese-addons

**作用**：提供拼音、双拼、五笔等常用中文输入方案。

**使用情境**：作为 Rime 之外的后备输入方案，或给不需要高度定制输入法的用户使用。

---

## 6. 截图与剪贴板

### 6.1 grim —— Wayland 截图工具

**作用**：在 Wayland 下进行屏幕截图，支持全屏或区域。

**使用情境**：
- `grim screenshot.png` → 全屏截图
- `grim -g "$(slurp)" screenshot.png` → 选择区域截图

### 6.2 slurp —— 区域选择工具

**作用**：在 Wayland 下用鼠标拖拽选择一个矩形区域。

**使用情境**：
- 配合 grim：`grim -g "$(slurp)" - | wl-copy` → 选区截图直接进剪贴板
- 配合 OBS 选择录制区域

### 6.3 satty / swappy —— 截图编辑标注

**作用**：对截图进行标注（箭头、文字、矩形框、模糊等）。

**使用情境**：
- 截图后立即进入编辑界面 → 圈出重点内容 → 保存或复制
- 给 bug 报告标注问题区域
- 隐藏截图中的敏感信息（用模糊/马赛克工具）

**本项目配置位置**：
- satty 截图脚本（新版 Niri）：`legacy/.config/niri/scripts/satty-screenshot.sh`
- swappy 截图脚本（旧版 Hyprland）：`legacy/.config/hypr/scripts/edit-screenshot.sh`

### 6.4 wl-clipboard —— 命令行剪贴板

**作用**：在命令行和 Wayland 剪贴板之间传输数据。

**使用情境**：
- `echo "hello" | wl-copy` → 把文本复制到剪贴板
- `wl-paste` → 输出当前剪贴板内容
- `wl-paste --watch cliphist store` → 自动记录剪贴板历史

**本项目**：Niri 配置中自动启动 `wl-paste --watch cliphist store` 记录剪贴板历史。

### 6.5 cliphist —— 剪贴板历史

**作用**：记录剪贴板的所有历史内容，支持文本和图片。

**使用情境**：
- 复制了多段文字后，可选择粘贴之前某次复制的内容（而非最后一次）
- 配合 fuzzel 选择历史条目

### 6.6 copyq —— 高级剪贴板管理器

**作用**：GUI 剪贴板管理，支持编辑、搜索、标签、脚本处理。

**使用情境**：
- 查看最近 50 条剪贴板记录 → 搜索 → 双击粘贴
- 对复制的内容进行预处理（去除格式、加密等）

**本项目**：旧 Hyprland 配置中通过 `exec-once = copyq` 自启动。

---

## 7. 硬件控制与媒体

### 7.1 brightnessctl —— 亮度控制

**作用**：调节显示器背光亮度和键盘背光。

**使用情境**：
- 按笔记本亮度键（`XF86MonBrightnessUp/Down`）→ 自动调节
- `brightnessctl set 50%` → 手动设为 50% 亮度

**本项目**：配置在 Hyprland 的多媒体键绑定中。

### 7.2 playerctl —— 媒体播放控制

**作用**：统一控制所有媒体播放器（Spotify、浏览器视频、本地音乐等）。

**使用情境**：
- 按键盘多媒体键（播放/暂停/上一首/下一首）→ 控制当前播放器
- `playerctl metadata` → 查看当前播放曲目信息

**本项目**：配置在 Hyprland 的多媒体键绑定中。

### 7.3 pavucontrol —— 音频控制面板

**作用**：图形化的 PulseAudio/PipeWire 音量控制，管理每个应用的音量和输入输出设备。

**使用情境**：
- 浏览器音量太大，单独把它调低
- 切换扬声器到蓝牙耳机
- 检测麦克风是否正常工作

---

## 8. 开发与文本工具

### 8.1 vim / neovim / nano

**作用**：终端文本编辑器。vim/neovim 面向高级用户，nano 面向初学者。

**使用情境**：
- `nvim ~/.config/niri/config.kdl` → 编辑 Niri 配置
- `sudo nano /etc/fstab` → 简单编辑系统文件

### 8.2 ripgrep (rg)

**作用**：极快的文件内容搜索工具，递归搜索目录中的文本。

**使用情境**：
- `rg "exec_line" ~/Projects/` → 快速找到所有包含 exec_line 的文件
- `rg -l "TODO" src/` → 列出所有包含 TODO 标记的文件

### 8.3 jq —— JSON 处理器

**作用**：命令行解析、过滤、转换 JSON 数据。

**使用情境**：
- `curl -s api.github.com/repos/... | jq '.stargazers_count'` → 提取 GitHub star 数
- waybar 的 JSON 配置验证

### 8.4 git

**作用**：版本控制系统。

**使用情境**：
- `git clone` 下载项目
- `git pull` 更新脚本
- 管理自己的配置文件版本

---

## 9. 游戏与性能监控

### 9.1 MangoHud —— 性能叠加层

**作用**：在游戏中显示 FPS、帧时间、CPU/GPU 占用率、温度、功耗等实时性能数据。

**使用情境**：
- 启动游戏时自动显示左上角性能监控
- 排查游戏卡顿 → 看 FPS 曲线和帧时间
- 测试不同画质设置对性能的影响

**本项目配置位置**：`legacy/.config/MangoHud/MangoHud.conf`

### 9.2 mango (MangoJuice) —— MangoHud 配置工具

**作用**：图形化配置 MangoHud 的显示内容和样式。

**本项目配置位置**：`legacy/.config/mango/config.conf`

---

## 10. 主题与外观

### 10.1 qt5ct / qt6ct —— Qt 主题配置

**作用**：统一 Qt 应用的外观主题（因为在 GTK 桌面上运行 Qt 应用会显得格格不入）。

**使用情境**：
- 设置 Qt 应用使用与 GTK 一致的配色方案
- 选择图标主题

### 10.2 Matugen —— 主题色彩生成器

**作用**：从当前壁纸自动提取色彩方案，生成与应用主题匹配的配色文件。

**使用情境**：
- 切换壁纸后自动更新整个桌面的配色
- 统一 GTK 主题、终端配色、waybar 样式的色调

**本项目配置位置**：`legacy/.config/ghostty/themes/Matugen`

### 10.3 niri_auto_blur_bg.sh —— 背景自动模糊

**作用**：当有窗口打开时自动模糊桌面背景，窗口最小化后恢复清晰。

**使用情境**：
- 打开浏览器 → 桌面壁纸自动模糊，突出前景窗口
- 关闭所有窗口 → 壁纸恢复清晰

**本项目配置位置**：`legacy/.config/niri/scripts/niri_auto_blur_bg.sh`

---

## 11. Arch/Hyprland 专属（legacy）

以下是原 Arch 一键脚本中使用的工具，目前保留在 `legacy/` 目录中，Debian 版本部分替换或移除了它们。

### 11.1 Hyprland —— 动态平铺 Wayland 合成器

**作用**：原项目的主力桌面，类似 i3 的平铺逻辑 + 丰富的动画和视觉效果。

**本项目配置位置**：`legacy/.config/hypr/hyprland.conf`（完整配置，含 373 行按键绑定、窗口规则、动画参数等）

### 11.2 Hyprland 插件

| 插件 | 作用 |
|------|------|
| **hyprscrolling** | 滚动式平铺布局（类似 Niri 的布局逻辑，故迁移到 Niri 后不再需要） |
| **hyprexpo** | 工作区总览/鸟瞰视图 (`Mod+G`) |

### 11.3 swww —— 动态壁纸守护进程

**作用**：支持 GIF/视频壁纸，以及壁纸切换过渡动画。

**使用情境**：
- 设置 GIF 动图作为桌面壁纸
- 定时自动更换壁纸，有淡入淡出效果

### 11.4 waypaper —— 壁纸选择器

**作用**：GUI 界面浏览和切换壁纸。

**使用情境**：
- `Mod+Alt+W` 打开壁纸管理器 → 浏览文件夹 → 双击切换

### 11.5 VFIO 显卡直通切换器

**作用**：在 Linux 宿主和 Windows 虚拟机之间切换 GPU 的使用权（用于 KVM 显卡直通）。

**本项目配置位置**：`legacy/useful_tools/vfio-switcher.sh`

---

## 12. 配置目录速查表

| 工具 | 本项目配置位置 | 系统安装后位置 |
|------|---------------|---------------|
| **Niri** | `scripts/debian-install.sh` (模板) | `~/.config/niri/config.kdl` |
| **Niri 截图脚本** | `legacy/.config/niri/scripts/` | `~/.config/niri/scripts/` |
| **Waybar** | `scripts/debian-install.sh` (模板) | `~/.config/waybar/config-niri`, `style.css` |
| **foot** | `legacy/.config/foot/foot.ini` | `~/.config/foot/foot.ini` |
| **ghostty** | `legacy/.config/ghostty/config` | `~/.config/ghostty/config` |
| **MangoHud** | `legacy/.config/MangoHud/MangoHud.conf` | `~/.config/MangoHud/MangoHud.conf` |
| **mango** | `legacy/.config/mango/config.conf` | `~/.config/mango/config.conf` |
| **niriswitcher** | `legacy/.config/niriswitcher/` | `~/.config/niriswitcher/` |
| **wlogout** | `legacy/.config/wlogout/` | `~/.config/wlogout/` |
| **fcitx5/rime** | `legacy/.local/share/fcitx5/rime/` | `~/.local/share/fcitx5/rime/` |
| **fcitx5 env** | `scripts/debian-install.sh` (模板) | `/etc/profile.d/shorin-fcitx5.sh` |
| **zsh** | `legacy/.zshrc` | `~/.zshrc` |
| **Hyprland** | `legacy/.config/hypr/hyprland.conf` | `~/.config/hypr/hyprland.conf` |
| **hypridle** | `legacy/.config/hypr/hypridle.conf` | `~/.config/hypr/hypridle.conf` |
| **Hyprland 脚本** | `legacy/.config/hypr/scripts/` | `~/.config/hypr/scripts/` |
| **壁纸** | `wallpapers/` | `~/Pictures/Shorin-Wallpapers/` |

---

## 附录：键盘快捷键速查（Niri 默认）

本项目生成的 Niri 配置中包含以下核心快捷键（`Mod` = Super/Windows 键）：

| 快捷键 | 功能 |
|--------|------|
| `Mod+T` | 打开终端 (kitty) |
| `Mod+B` | 打开浏览器 (Firefox) |
| `Mod+Z` | 打开应用启动器 (fuzzel) |
| `Mod+E` | 打开文件管理器 (thunar) |
| `Mod+Q` | 关闭当前窗口 |
| `Mod+V` | 切换窗口浮动模式 |
| `Mod+F` | 最大化列 |
| `Mod+Shift+E` | 退出 Niri |
| `Mod+1~9` | 切换到工作区 1~9 |
| `Mod+Ctrl+1~9` | 移动窗口到工作区 1~9 |
| `Mod+H/L` | 左右移动焦点 |
| `Mod+J/K` | 上下移动焦点 |
| `Mod+Ctrl+H/L/J/K` | 移动窗口 |
| `Mod+R` | 切换列宽预设 |
| `Mod+Shift+S` | 截图并进入编辑 |
| `Mod+Alt+A` | 选区截图到剪贴板 |
| `Mod+Alt+L` | 锁屏 |
| `Mod+O` | 刷新 waybar |
